import {ReactNode} from "react";

export type ISlideProps = {
    children: ReactNode
    power: number
    view?: boolean
    className?: string
}
export type IExitAnimateProps = {
    children: ReactNode
}